<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Colors -->
    <Color x:Key="DataGridHeaderBackground">#F5F5F5</Color>
    <Color x:Key="DataGridHeaderForeground">#212121</Color>
    <Color x:Key="DataGridRowBackground">#FFFFFF</Color>
    <Color x:Key="DataGridRowAlternatingBackground">#FAFAFA</Color>
    <Color x:Key="DataGridRowHoverBackground">#E3F2FD</Color>
    <Color x:Key="DataGridRowSelectedBackground">#2196F3</Color>
    <Color x:Key="DataGridRowSelectedForeground">#FFFFFF</Color>
    <Color x:Key="DataGridBorderColor">#E0E0E0</Color>

    <!-- Brushes -->
    <SolidColorBrush x:Key="DataGridHeaderBackgroundBrush" Color="{StaticResource DataGridHeaderBackground}"/>
    <SolidColorBrush x:Key="DataGridHeaderForegroundBrush" Color="{StaticResource DataGridHeaderForeground}"/>
    <SolidColorBrush x:Key="DataGridRowBackgroundBrush" Color="{StaticResource DataGridRowBackground}"/>
    <SolidColorBrush x:Key="DataGridRowAlternatingBackgroundBrush" Color="{StaticResource DataGridRowAlternatingBackground}"/>
    <SolidColorBrush x:Key="DataGridRowHoverBackgroundBrush" Color="{StaticResource DataGridRowHoverBackground}"/>
    <SolidColorBrush x:Key="DataGridRowSelectedBackgroundBrush" Color="{StaticResource DataGridRowSelectedBackground}"/>
    <SolidColorBrush x:Key="DataGridRowSelectedForegroundBrush" Color="{StaticResource DataGridRowSelectedForeground}"/>
    <SolidColorBrush x:Key="DataGridBorderBrush" Color="{StaticResource DataGridBorderColor}"/>

    <!-- DataGridCell Style -->
    <Style x:Key="ModernDataGridCellStyle" TargetType="DataGridCell">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="#212121"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource DataGridRowSelectedBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource DataGridRowSelectedForegroundBrush}"/>
            </Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter Property="BorderThickness" Value="0"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- DataGridColumnHeader Style -->
    <Style x:Key="ModernDataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource DataGridHeaderBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource DataGridHeaderForegroundBrush}"/>
        <Setter Property="Padding" Value="12,10"/>
        <Setter Property="BorderBrush" Value="{StaticResource DataGridBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridColumnHeader">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <ContentPresenter/>
                            <Path x:Name="SortIndicator" 
                                  Margin="8,0,0,0"
                                  Data="M 0,0 L 4,4 L 8,0 Z" 
                                  Fill="{StaticResource DataGridHeaderForegroundBrush}"
                                  Visibility="Collapsed"
                                  VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#EEEEEE"/>
                <Setter Property="Cursor" Value="Hand"/>
            </Trigger>
            <Trigger Property="SortDirection" Value="Ascending">
                <Setter Property="Background" Value="#E0E0E0"/>
            </Trigger>
            <Trigger Property="SortDirection" Value="Descending">
                <Setter Property="Background" Value="#E0E0E0"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- DataGridRow Style -->
    <Style x:Key="ModernDataGridRowStyle" TargetType="DataGridRow">
        <Setter Property="Background" Value="{StaticResource DataGridRowBackgroundBrush}"/>
        <Setter Property="Foreground" Value="#212121"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="MinHeight" Value="36"/>
        <Style.Triggers>
            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                <Setter Property="Background" Value="{StaticResource DataGridRowAlternatingBackgroundBrush}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource DataGridRowHoverBackgroundBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource DataGridRowSelectedBackgroundBrush}"/>
                <Setter Property="Foreground" Value="{StaticResource DataGridRowSelectedForegroundBrush}"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True"/>
                    <Condition Property="IsMouseOver" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="#1976D2"/>
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <!-- Main DataGrid Style -->
    <Style x:Key="ModernDataGridStyle" TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource DataGridRowBackgroundBrush}"/>
        <Setter Property="Foreground" Value="#212121"/>
        <Setter Property="BorderBrush" Value="{StaticResource DataGridBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="RowStyle" Value="{StaticResource ModernDataGridRowStyle}"/>
        <Setter Property="CellStyle" Value="{StaticResource ModernDataGridCellStyle}"/>
        <Setter Property="ColumnHeaderStyle" Value="{StaticResource ModernDataGridColumnHeaderStyle}"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource DataGridBorderBrush}"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="CanUserReorderColumns" Value="True"/>
        <Setter Property="CanUserResizeColumns" Value="True"/>
        <Setter Property="CanUserSortColumns" Value="True"/>
        <Setter Property="SelectionMode" Value="Extended"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="FontSize" Value="12"/>
    </Style>

    <!-- Scrollbar for DataGrid -->
    <Style x:Key="DataGridScrollBarStyle" TargetType="ScrollBar">
        <Setter Property="Width" Value="10"/>
        <Setter Property="Background" Value="#F5F5F5"/>
    </Style>

</ResourceDictionary>